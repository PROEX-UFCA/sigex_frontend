export const CATEGORIES = ["Cultura", "Tecnologia", "Educação"] // adicionar mais caso necessário
export type Category = typeof CATEGORIES[number];

export const BREAKPOINTS = {
  small: 768,
  medium: 1024
}